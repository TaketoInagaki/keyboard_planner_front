name: keyboard_planner_front
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  provider: ^5.0.0
  freezed_annotation:
  state_notifier: ^0.5.0


  cupertino_icons: ^1.0.2
  dio: ^4.0.6
  google_fonts: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
